# =============================================================================
# Industrial Communication Simulator - Requirements
# =============================================================================
# Install all: pip install -r requirements.txt
# Install specific protocols: pip install -r requirements.txt --no-deps
# =============================================================================

# Core (required)
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Protocol-specific (optional)
paho-mqtt>=1.6.0        # MQTT + Sparkplug
pymodbus>=3.0.0         # Modbus TCP/RTU
bacpypes>=0.18.0        # BACnet/IP
opcua-asyncio>=1.0.0    # OPC UA
python-snap7>=1.3.0     # Siemens S7
protobuf>=4.0.0         # Sparkplug B protobuf

# Development
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
pre-commit>=3.0.0